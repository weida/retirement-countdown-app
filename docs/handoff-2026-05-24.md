# 交接快照 · 2026-05-24

## TL;DR

- **当前是自用 app，不上任何应用商店**。所有"上架前要做的事"全部暂停。
- 分支 `fix/restore-reminder-and-wire-share` 已 push 到远端，含本轮 17 个 commit。工作目录干净。
- 最新 debug APK：`~/Downloads/retirement-countdown/latest/retirement-countdown-debug-apk/app-debug.apk` (~4.7M)。直接装机即可，覆盖装会保留 localStorage 设置。

## 本轮交付的功能

按合并顺序（旧到新）：

1. `547333b` `2328307` `e879617` —— 此前的 rolling digit / 滑块溢出修复（前置）
2. `f2d043e` **fix(hero)** —— 数字 + "天" 居中微调；4 位数自动降字号
3. `d23e43e` **feat(settings)** —— 退休日期改为 3 列自定义 picker + 空提交校验 + 任意 part 即清提示
4. `6f31bf5` **feat(settings)** —— 提醒时间改为 2 列 picker（5 分钟就近取整 + 跨小时进位） + 警告软化
5. `37b3f6d` **feat(android)** —— V5 极简倒计时环图标（同事 design/share-poster 出的 SVG，cherry-pick）
6. `7b4b055` **fix(pwa)** —— `public/icon.svg` 同步到 V5
7. `d9b0831` **fix(android)** —— Adaptive icon 前景/背景分层（解决某些 launcher 白边）
8. `6627df1` **fix(android)** —— 去掉 legacy `assets/icon.png` 的圆角与白角
9. `9491ddb` **feat(home)** —— hero 与 stats 之间加每日一句小签（80 条）
10. `b236ff0` —— 小签池子扩到 365 条，按主题分桶
11. `9e414e0` **feat(status)** —— 抽离 `formatCountdownStatus`，主屏文案随 `remindersEnabled` 切换
12. `399c68e` **fix(share)** —— `isShareCanceledError` 统一原生 / web 取消判定，取消静默
13. `0531786` **feat(share-poster)** —— 海报底部 = 主屏当天小签，footer 用真实 status；同步 polish 15 条小签措辞
14. `bdafaf8` **chore(repo)** —— 提交一直没 commit 的 deps + AGENTS.md
15. `29938b8` **chore(release)** —— `appId` 从 `com.codex.retirementcountdown` 改为 `dev.weicao.retirementcountdown`
16. `b67bf58` **ci(android)** —— cherry-pick PR #5 的 signed release AAB 流水线（无 keystore 时自动跳过，dormant 不打扰 debug 流程）

## 仓库 / CI 状态

- 远端分支：`origin/fix/restore-reminder-and-wire-share`
- PR #6 还开着（标题 `fix(main): restore reminder scheduling and wire share button`），自动追踪本分支
- `package.json` 锁定 deps：`@capacitor/assets` (CI 图标生成) + `@playwright/test` (review 烟测)
- CI workflow 触发口：push 到 `main`/`master` 或 `gh workflow run android-apk.yml --ref <branch>`
- 测试：`npm test` 16/16，覆盖 retirement policy + share cancel 判定 + countdown 状态文案

## 还没做的事（已搁置）

这些都是"上架"才需要的，自用不要做：

- 生成 release keystore + 配 GitHub Secrets（`ANDROID_KEYSTORE_BASE64` / `ANDROID_KEYSTORE_PASSWORD` / `ANDROID_KEY_ALIAS` / `ANDROID_KEY_PASSWORD`）
- 写隐私政策 + 找 URL 托管（GitHub Pages 最省事）
- Google Play / 国内商店任何注册、软著、备案、实名

## 接着做的话怎么开局

```bash
cd ~/github/retirement-countdown-app
git checkout fix/restore-reminder-and-wire-share
git pull
npm install
npm run dev   # 浏览器开 http://127.0.0.1:4173/
```

CI 出新 APK：

```bash
gh workflow run android-apk.yml --ref fix/restore-reminder-and-wire-share
# 等 ~2 min,然后:
gh run download <run-id> -R weida/retirement-countdown-app -D ~/Downloads/retirement-countdown/latest
```

或者直接看最近一次：

```bash
gh run list --workflow=android-apk.yml --branch=fix/restore-reminder-and-wire-share --limit=1
```

## 关键文件索引

- `src/main.js` —— DOM/状态主控；新加的入口：`setHeroCount` (line ~1340)、`updateDailyLine` (~1750)、`generateShareImage` (~2130)、`drawSharePoster` (~2190)
- `src/retirement.js` —— 计算政策；新加 `formatCountdownStatus`
- `src/share.js` —— `isShareCanceledError`
- `assets/icon.png` `assets/icon-foreground.png` `assets/icon-background.png` —— 三层图标，CI 经 `@capacitor/assets` 转换
- `capacitor.config.json` —— `appId: dev.weicao.retirementcountdown`
- `.github/workflows/android-apk.yml` —— Build pipeline，含 dormant 的 signing 步骤
- `scripts/android-inject-signing.mjs` —— CI signing helper（来自 PR #5）

## 本地环境备忘

- Pi 上 vite dev server 通过 `npm run dev` 起在 4173；`192.168.2.6:4173` 暴露给局域网
- 防火墙：ufw 已对 `192.168.2.0/24` 子网放行 4173/tcp
- 备份位置：`/tmp/final-state-r2/`（这次工作的关键 src 文件镜像，会被 OS 清理，长期看 git 才是 source of truth）
